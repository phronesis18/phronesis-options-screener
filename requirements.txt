# ============================================================
# OPTIONS SCREENER PHRONESIS — Dépendances Python
# ============================================================

# Interface utilisateur
streamlit>=1.35.0
plotly>=5.24.0

# Data & calculs fondamentaux
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# Données de marché (fallback principal)
yfinance>=0.2.28

# Connexion Interactive Brokers (optionnel, fallback)
ib_insync==0.9.86

# Configuration & variables d'environnement
python-dotenv>=1.0.0

# Requêtes HTTP (DeepSeek, Twelve Data, Alpha Vantage)
requests>=2.31.0

# Formatage console (optionnel)
rich>=13.0.0

# Planification (optionnel)
schedule>=1.2.0

# Export Excel (optionnel)
openpyxl>=3.1.0